# List of packages to install.
# Each package is listed on a newline.
# Blank lines and lines starting with `#` are not included.

# Build essentials
build-essential

# Python (these probably change with each distro bump)
#python3.12-venv
python3-dev
python3-venv

